# 家庭智能药品管理应用原型设计需求文件 (PRD)

## 1. 产品概述与核心价值
本项目旨在解决家庭药品管理中“找药难（功效不明确）”、“定位难（存放位置无序）”、“效期管理难（过期不知情）”的痛点。通过**扫码/拍照识别**与**语音/文字双模搜索**，极度压缩用户的录入与查找成本，打造生病紧急场景下“听得懂、找得快、看得清”的智能药箱。

---

## 2. 核心功能需求清单

### 2.1 药箱空间管理（基础配置）
*   **空间增删改查：** 用户可自定义家庭中的物理药箱位置（如：客厅大药箱、冰箱冷藏区、主卧床头柜）。
*   **空间卡片化：** 每个位置作为一个独立的“虚拟药箱”容器，支持查看该位置下的药品数量及异常状态（如有几款过期）。

### 2.2 极速药品录入（解决输入痛点）
*   **扫条形码录入（核心）：** 
    *   点击调用摄像头扫描药盒条码，自动请求云端数据库。
    *   **自动填充字段：** 药品通用名、商品名、生产厂家、主治功能（说明书摘要）。
*   **拍照 OCR 识别（辅助）：** 
    *   针对无法扫码的药盒或医院代煎药，拍照识别药盒文字。
    *   **智能提取：** 自动抓取并填充“有效期至（EXP）”及“生产日期”。
*   **信息补齐表单：** 
    *   **必填项：** 存放位置（下拉选择，单选）、效期（若OCR未识别则手动选日期）。
    *   **选填项：** 开封状态（开关：未开封/已开封）。若选已开封，展开字段“开封日期”与“开封后有效天数”。

### 2.3 状态看板（时间与空间可视化）
*   **三色效期看板：** 
    *   **过期（红色）：** 当前时间 >= 过期日期。
    *   **临期（黄色）：** 当前时间距离过期日期 <= 30天（天数可配置）。
    *   **安全（绿色）：** 处于安全期内的药品。
*   **按位置盘点：** 首页支持按“药箱位置”切换，直观看到各个药箱抽屉里具体的药品清单。

### 2.4 智能双模搜索（解决紧急用药痛点）
*   **语音输入（ASR）：** 首页置顶搜索框，右侧设麦克风图标。按住说话，实时转文字（如：“我肚子疼”、“有感冒药吗”）。
*   **语义/标签匹配：** 系统需将输入的文本去匹配药品的“主治功能”或“功效标签”，支持模糊搜索。
*   **强视觉反馈结果页：** 搜索结果卡片必须**大字号、高亮**突出核心三要素：**【药品名称】**、**【存放位置】**、**【效期状态/剩余数量】**。

---

## 3. 页面原型与交互设计布局

### 页面一：首页（看板与搜索中心）
*   **顶部（全局搜索区）：** `[ 输入框：搜索药品名、功效或症状 ]` ＋ `[ 🎙️ 语音麦克风图标 ]`
*   **中部（效期统计仪表盘）：** 三个大色块横向排列，数字加粗：
    *   红色方块：已过期 X 种（点击进入过期列表，方便清理）
    *   黄色方块：临期 Y 种
    *   绿色方块：安全 Z 种
*   **下部（空间药箱列表）：** 列表或宫格展示：【客厅大药箱（5件）】、【冰箱冷藏区（2件）】。点击任意药箱，展开内部药品清单。
*   **底部导航栏（Tabbar）：** `[ 首页 ]` 、 `[ ➕ 极速录入（高亮主按钮） ]` 、 `[ 我的/设置 ]`

### 页面二：极速录入页（表单交互）
*   **顶部：** `[ 📷 点击扫码 / 拍照识别 ]` 巨幅按钮。
*   **中部（自动生成的药品名片）：**
    *   药品名称：`[ 自动带出，支持微调 ]`
    *   主治功能：`[ 自动带出，多标签展示，如：感冒、退烧 ]`
*   **下部（人工确认区）：**
    *   `*存放位置`：`[ 下拉选择框：请选择药箱 ]`（强校验）
    *   `*有效期至`：`[ 日期选择器 ]`（若OCR识别成功则默认填充）
    *   `开封管理`：`[ 开关：是否已开封 ]` -> 打开后联动出现 `[ 开封日期选择 ]`。

### 页面三：搜索结果页（极端紧急场景）
*   **头部：** 显示用户输入的语音/文字（如：*“我胃痛”*）。
*   **核心列表（卡片式设计，按匹配度排序）：**
    *   左侧：药品外观缩略图/剂型图标（如胶囊/液体）。
    *   中间：**药品名称**（大字号）、主治功能标签（如：胃痛、制酸）。
    *   右侧/底部高亮：**位置：客厅电视柜第三个抽屉**（大字号、加粗、醒目的主题色）。
    *   效期状态标签：显示“正常（剩12粒）”或“已过期（红色警告）”。

---

## 4. 核心业务流程

### 流程一：药品入库（扫码录入）
1. 用户点击底部【➕】按钮 -> 触发相机。
2. 对准条码“嘀”一下 -> 云端匹配药品信息成功，自动填表。
3. 用户下拉选择存放位置（如“主卧抽屉”）并确认效期。
4. 点击【完成保存】，药品进入数据库。

### 流程二：紧急找药（语音搜索）
1. 用户身体不适，打开应用点击首页【🎙️】图标。
2. 按住说话：“我拉肚子，有什么药吗？” -> 系统进行语音转文字及语义转译。
3. 页面瞬间弹出相关药品“整肠生”结果卡片。
4. 视线聚焦高亮字：**【位置：冰箱第二层】**，用户快速取药。

---

## 5. 后台数据与非功能需求
*   **基础字典：** 系统需预置一套常见的“症状-功效标签”映射表（如：头痛/发烧/牙痛 -> 匹配“止痛”、“退烧”功效）。
*   **本地缓存/离线可用：** 考虑到断网或弱网情况，家庭位置数据及已下载的药品说明书应支持本地缓存，确保无网也能通过文字搜到具体位置。
*   **主治功能文本精简：** 扫码拉取的官方说明书通常长篇大论，前端展示时必须经过文本截取（建议只展示前50个字或直接提取关键词标签），避免撑破原型界面。